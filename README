# 🤖 AI Agent Learn

一个从零构建的 AI Agent 项目，具备**多工具调用**、**RAG 知识库检索**和 **Web 交互界面**。

## 项目简介

本项目实现了一个完整的 AI Agent 系统，核心遵循「感知 → 思考 → 行动」的智能体循环。Agent 能够根据用户的自然语言输入，自主判断是否需要调用外部工具，并基于工具返回的结果生成最终回答。

### 核心能力

- **🌤️ 实时天气查询** — 对接和风天气 API，支持全国城市天气查询
- **🔢 数学计算** — 支持任意数学表达式的计算
- **💼 岗位信息查询** — 查询不同城市和方向的招聘信息
- **📚 RAG 知识库问答** — 基于 ChromaDB 向量数据库，从本地知识库中检索相关内容并回答问题
- **💬 多轮对话记忆** — 支持上下文连续对话，Agent 能理解指代和省略
- **🖥️ Web 交互界面** — 基于 Gradio 的浏览器端交互界面

## 技术架构

```
用户输入
  │
  ▼
Agent 核心循环
  │
  ├─ 1. 构建消息（含对话历史）
  ├─ 2. 发送给 LLM（DeepSeek API）
  ├─ 3. LLM 决策：是否需要工具？
  │     │
  │     ├─ 是 → 调用工具 → 获取结果 → 再次调用 LLM 生成回答
  │     └─ 否 → 直接生成回答
  │
  └─ 4. 返回结果，更新对话历史
```

### 技术栈

| 组件 | 技术选型 |
|------|----------|
| 大模型 | DeepSeek API（OpenAI 兼容格式） |
| 向量数据库 | ChromaDB（本地持久化） |
| Embedding | all-MiniLM-L6-v2（ChromaDB 内置） |
| 天气 API | 和风天气 API |
| Web 界面 | Gradio |
| 开发语言 | Python 3.12 |

## 项目结构

```
ai-agent-learn/
├── agent_v1.py           # Agent 核心代码（命令行版）
├── web_app.py            # Agent Web 界面版
├── build_knowledge.py    # 知识库构建脚本（文档切分 + 向量化）
├── search_knowledge.py   # 向量检索测试脚本
├── knowledge.txt         # 知识库源文档
├── requirements.txt      # 项目依赖
├── .env                  # 环境变量（API Key，不纳入版本控制）
└── chroma_db/            # ChromaDB 本地存储（自动生成）
```

## 快速开始

### 1. 克隆项目

```bash
git clone https://github.com/yuangao93/ai-agent-learn.git
cd ai-agent-learn
```

### 2. 安装依赖

```bash
python -m venv .venv
# Windows
.venv\Scripts\activate
# macOS/Linux
source .venv/bin/activate

pip install -r requirements.txt
```

### 3. 配置环境变量

在项目根目录创建 `.env` 文件：

```env
DEEPSEEK_API_KEY=你的DeepSeek_API_Key
WEATHER_API_HOST=你的和风天气API域名
WEATHER_API_KEY=你的和风天气API_Key
```

- DeepSeek API Key 获取：[platform.deepseek.com](https://platform.deepseek.com)
- 和风天气 API Key 获取：[dev.qweather.com](https://dev.qweather.com)

### 4. 构建知识库

```bash
python build_knowledge.py
```

### 5. 运行

**命令行模式：**
```bash
python agent_v1.py
```

**Web 界面模式：**
```bash
python web_app.py
# 浏览器访问 http://127.0.0.1:7860
```

## 实现细节

### Tool Use（工具调用）

Agent 通过 OpenAI 兼容的 Function Calling 协议，将可用工具以 JSON Schema 的形式注册给大模型。大模型根据用户意图自主决定是否调用工具以及调用哪个工具。

### RAG（检索增强生成）

1. **文档切分**：将知识库文档按固定长度切分为多个文本段，相邻段之间保留重叠以避免语义断裂
2. **向量化存储**：使用 ChromaDB 内置的 all-MiniLM-L6-v2 模型将文本段转为向量并持久化存储
3. **语义检索**：用户提问时，将问题转为向量，通过余弦相似度找到最相关的文本段
4. **增强生成**：将检索到的相关文本段和用户问题一起发送给大模型，生成基于知识库的回答

### 对话记忆

使用全局对话历史列表维护上下文，每轮对话（包括工具调用过程）都记录在内，实现连续多轮对话能力。

## 后续计划

- [ ] 接入更多真实数据源（招聘 API、新闻 API 等）
- [ ] 支持多文档格式知识库（PDF、Word、Markdown）
- [ ] 实现 Multi-Agent 协作
- [ ] 添加流式输出（Streaming）
- [ ] 部署到云端

## License

MIT